{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter=@medialit/thumbnail build && pnpm --filter=@medialit/images build && pnpm --filter=@medialit/utils build && pnpm --filter=@medialit/models build && pnpm --filter=@medialit/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter=@medialit/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}